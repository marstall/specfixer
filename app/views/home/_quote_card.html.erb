<div class="quote-card">
  <% if @quote_of_the_day %>
    <blockquote>
      <%= @quote_of_the_day.content %>
    </blockquote>
    <cite>— <%= @quote_of_the_day.author %></cite>
  <% else %>
    <p>No quote available</p>
  <% end %>
</div>